module.exports = {
  routes: [
    "Portal ? Inventory",
    "Portal ? POS",
    "Portal ? Cloud",
    "Portal ? Academy",
    "Portal ? AlNour",
    "Portal ? Website",
    "Portal ? Profile",
    "Portal ? Portfolio"
  ]
};
